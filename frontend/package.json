{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "vue-tsc -b",
    "build:android": "vue-tsc -b && vite build --mode android",
    "build:android:release": "vue-tsc -b && vite build --mode production",
    "android:sync": "npm run android:sync:debug",
    "android:sync:debug": "npm run build:android && CAPACITOR_ANDROID_LOCAL=1 cap sync android && npm run android:pin-agp",
    "android:sync:release": "npm run build:android:release && cap sync android && npm run android:pin-agp",
    "android:open": "npm run android:sync && cap open android",
    "android:build": "npm run android:sync && cd android && ./gradlew assembleDebug",
    "android:pin-agp": "node scripts/pin-android-gradle-plugin.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/core": "^8.3.1",
    "laravel-echo": "^2.3.4",
    "pinia": "^3.0.4",
    "pusher-js": "^8.5.0",
    "vue": "^3.5.32",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.1",
    "@pinia/testing": "^1.0.3",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/vue": "^8.1.0",
    "@types/node": "^24.12.2",
    "@vitejs/plugin-vue": "^6.0.6",
    "@vue/test-utils": "^2.4.9",
    "@vue/tsconfig": "^0.9.1",
    "jsdom": "^29.1.0",
    "typescript": "~6.0.2",
    "vite": "^8.0.10",
    "vitest": "^4.1.5",
    "vue-tsc": "^3.2.7"
  }
}
